/**
 * Client-side exports for Next.js (uses @ciph/react under the hood).
 * Re-exports provider, hooks, and utilities for use in Next.js client components.
 */

export { CiphProvider, useCiph, createClient } from "@ciph/react"
export type { CiphProviderProps } from "@ciph/react"
export type { CiphClientEmitter } from "@ciph/react"

